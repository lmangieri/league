pipeline {
	agent any
	tools {
        maven 'maven_3_5_4' 
    }
	stages  {
		stage('Compile Stage') {
			steps {
				sh "mvn clean install"
			}
		}
		stage('Testing Stage') {
			steps {
				sh "mvn test"
			}
		}
	//	stage('Deployment Stage') { test
	//		steps {
	//			sh "mvn deploy"
	//		}
	//	} 
	}
}